name: Go migration readiness

on:
  push:
  pull_request:

jobs:
  go-ready:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - run: zig build go-ready --summary failures
